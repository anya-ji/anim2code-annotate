[project]
name = "anim2code-annotate"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "firebase-admin>=6.5",
    "pandas>=2.2",
    "requests>=2.32",
    "huggingface-hub>=0.24",
]
